One-off patch scripts from early development (lambda capture fixes, worker
init ordering, aggregation lambda corrections). All fixes are already
permanently applied to the real source files under include/ and
feature_flow/. Kept for historical reference only — not used by any build
or run process.
